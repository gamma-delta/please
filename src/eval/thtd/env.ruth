(define let (macro (bindings ?bindings/body . body)
    (if symbol? bindings)
        (todo)
        (apply 
            `(lambda 
                ,(for bindings car) 
                ,(cons ?bindings/body body))
            (apply eval (for bindings second))))
