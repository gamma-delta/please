(define (add1 x) (+ x 1))
(define (sub1 x) (- x 1))

(define-macro (cond . bodies)
  (if bodies
    (let ([test (car bodies)])
      `(if ,(first test)
        ,(second test)
        (cond . ,(cdr bodies))))
    '(! "did not find a true condition" '())))
