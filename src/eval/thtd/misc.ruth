(define-macro (do . bodies) (eval (append '(let ()) bodies)))

(define-macro (~> bodies) `(fold ,(car bodies) ,(cdr bodies)
  (lambda (threaded body) ())))

; mm yes,,  ergonomics
(define-macro (examine . bodies) `',bodies)
